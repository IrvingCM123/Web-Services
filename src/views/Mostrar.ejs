<!DOCTYPE html>
<html lang="en">

<head>
  <%- include("partials/header.ejs")%>
    <%- include("partials/estilos.ejs")%>
    <title>Document</title>
</head>

<body>

  <div class="container p-4">
    <div class="row">
      <% users.forEach(datos => { %>
        <div class="col-md-4">
          <div class="card">

            <div class="card-header">
              <%= datos.IDUsuario %> / <%= datos.Nombre_Usuario%>
            </div>
            <div class="card-body">
              <%= datos.Apellido_Usuario %>
            </div>
            <div class="card-body">
              <%= datos.Correo_Electronico %>
            </div>
            <div class="card-body">
              <%= datos.Contraseña %>
            </div>
            <div class="card-body">
              <%= datos.Fecha_Creacion %>
            </div>
            <div class="card-body">
              <%= datos.Fecha_Baja %>
            </div>
            <div class="card-footer">
              <button class="btn btn-danger">
                Eliminar
              </button>
            </div>

          </div>
        </div>
        <% }) %>
    </div>

</body>

</html>